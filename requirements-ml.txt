# ML/CV dependencies for BHAIRAV
# Separate from base requirements for lighter Docker builds

# Object detection
ultralytics>=8.0.0
opencv-python-headless>=4.8.0

# Face detection/recognition (YuNet + SFace via OpenCV)
# These are bundled with opencv-contrib-python or downloaded at runtime

# Deep learning backends
torch>=2.0.0
torchvision>=0.15.0

# Numerical
numpy>=1.24.0
scipy>=1.10.0

# Tracking
# ByteTrack is included in ultralytics
