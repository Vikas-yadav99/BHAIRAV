global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "bhairav"
    static_configs:
      - targets: ["bhairav-server:8000"]
    metrics_path: "/api/metrics"
    scrape_interval: 10s
